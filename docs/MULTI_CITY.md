# 多城市经营与切换

## 军衔与城市名额

主城计入总数，正式晋升后开放名额，声望达标本身不会解锁。建设中的城市也占名额。

| 军衔 | 城市总上限 |
| --- | --- |
| 列兵、上等兵、下士 | 1 |
| 中士、上士、军士长 | 2 |
| 准尉、少尉、中尉 | 3 |
| 上尉、少校、中校 | 4 |
| 上校、大校 | 5 |
| 少将 | 6 |
| 中将 | 7 |
| 上将 | 8 |

## 建城

在导航栏「切换」中选择「建立新城」，选择自己占领的森林或丘陵。资源地和沼泽不可建城，仍有行军的地块不可转换。

费用从操作发起的城市扣除：粮食 5,000、钢铁 10,000、石油 5,000、稀矿 2,000、黄金 10,000。建设需30分钟。开始建设时，野地转为城市并占用一个名额；不能取消或放弃城市。

新城初始拥有1级市政厅、民居、农田和炼钢厂，50平民以及五种资源各1,000，无初始驻军和新手礼包。建成前不能进入、调遣或被进攻，建成后开始按城市时间结算。

## 切换与归属

导航采用每行 7 列、每页两行布局，「切换」位于「首页」后面，不再单独占用一行。悬停入口可查看当前城名与主城/分城标记。桌面端显示城市面板，窄屏显示底部面板。主城置顶，其他城市按建立顺序排列。列表显示等级、坐标、当前状态、建设倒计时及来袭提示。

点击城市行切换，无二次确认。普通城市功能停留在原功能页；地图定位到目标城；公共页面保持当前页面。操作草稿不会跨城保留。地图中的「进入城市」同样会选择对应城市。

各城独立：资源和黄金、人口、税率、民心、民怨、建筑、建造/训练队列、驻军、城防、军校招募、军官驻地与任命、伤兵及城市道具效果。

玩家共享：军衔、声望、钻石、物品和装备库存、科技、任务、战报、邮件、军团。科技沿用现有即时升级行为，升级成果全城共享，不新增按城研究进度。

在另一座已建成城市行旁选择「调遣」：

- 调遣：所选部队、资源和随行军官到达后留驻目标城。
- 运输：资源送达，部队及随行军官返回出发城。
- 资源需要卡车等有负重的部队运送；黄金也可运输。不同城市之间不能通过界面切换瞬移资源或部队。
- 市长需先解除任命才能出征；正在行军的军官不能再次派遣、任命或解雇。抵达/返城后恢复闲置，可重新任命。

本阶段只通过建城增加城市。「征服」仍能击败守军、获取资源和生成双方战报，但不转移真实玩家的城市归属；避免旧版地图所有者变更造成主城丢失或绕过名额。完整攻城占领规则另行设计。

## 数据兼容与边界

V32迁移增加玩家内的稳定城市序号：0表示原有主城，新增城市从1开始。旧资源、建筑、部队和队列全部默认归属序号0；原有玩家表的经济字段继续保存主城数据，分城经济字段保存于城市记录，避免复制或重置旧存档。旧地图记录只有核对实际主城坐标后才被标记为可管理城市。

所有城市按独立时钟持续生产并结算队列，无需在线查看。行军记录固定保存出发城序号；战斗和侦查以实际目标城定位防守资源/兵力，返城不读取当前界面选择。

每次请求携带发起时的 `X-City-Id`，服务端核对归属、建成状态并固定本次操作的城市。不同浏览器标签页可操作不同城市。最近选择保存于玩家账号，用于重新登录/刷新时恢复。切换时清除旧请求和地图缓存，后台推送带城市序号，避免旧城数据覆盖新城。

建城时锁定玩家名额和目标地块，所有费用、建城、地块转换在同一事务内提交。内部重新初始化会清理城市数据后建立主城；正式环境的重置入口仍沿用原有禁用规则。旧版单城存档不能覆盖已有多城账号。

## 验证

新增服务与接口测试覆盖所有17级军衔、建城限制/名额、城市数据隔离、共享科技/钻石、后台生产与训练、建造归属、跨城运输/调遣、返城归属、实际防守目标、伤兵救治、请求城市校验与跨标签页操作。

前端回归覆盖旧响应丢弃、操作草稿清理、战报保留、后台城市推送过滤及导航入口。独立MySQL验证V26至最新迁移、原余额保留与每城部队唯一约束；浏览器验证桌面/390px手机布局、切换、调遣及建城倒计时。
